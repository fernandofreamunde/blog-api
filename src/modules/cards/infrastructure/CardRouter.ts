import { Router } from "express";

const cardRouter = Router();

// const readCardsController = new Controller();
// cardRouter.post('/cards', Auth, readCardsController.handle);

export { cardRouter };
